import SwiftUI
import shared  // KMP shared framework

@main
struct iOSApp: App {
    
    init() {
        // Initialize AppContextHolder equivalent for iOS
        // KMP shared module initialization happens here
    }
    
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
